#database
spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:file:/tmp/businesses_dev;AUTO_SERVER=true;DB_CLOSE_ON_EXIT=FALSE
    username: sa