spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: git@bitbucket.org:TarekSaid/synergyspace-config.git
          clone-on-start: true
server:
  port: 8888